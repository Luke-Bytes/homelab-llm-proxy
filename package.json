{
  "name": "local-llm-proxy",
  "version": "1.0.0",
  "description": "A lightweight OpenAI-compatible proxy for local Ollama with model aliases and prompt injection.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "test:smoke": "node --test test/smoke/logic.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "fastify": "^5.2.1"
  }
}
